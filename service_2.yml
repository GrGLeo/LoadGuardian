service:
  LogTen:
    image: deretz/log_ten
    network: 
      - app-network
    ports: 
      - "8080:8080"
    envs:
      - $TEN

  LogThree:
    image: deretz/log_three

  LogFive:
    image: deretz/log_five
    network: 
      - app-network
    envs:
      - $FIVE

networks:
  app-network:
    driver: bridge
